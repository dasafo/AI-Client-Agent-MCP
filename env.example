# env.example
# Archivo de ejemplo para configurar las variables de entorno del proyecto
# Copia este archivo como .env y ajusta los valores según tu entorno

# Configuración de la Base de Datos PostgreSQL
# Estas variables definen la conexión a la base de datos principal
DB_USER=your_postgres_user          # Usuario de PostgreSQL
DB_PASSWORD=your_postgres_password  # Contraseña del usuario
DB_HOST=localhost                   # Host donde se ejecuta PostgreSQL (usa 'db' con Docker Compose)
DB_PORT=5432                        # Puerto de PostgreSQL (5432 por defecto, 5433 mapeado en Docker)
DB_NAME=your_database_name          # Nombre de la base de datos a utilizar

# URL de conexión a la base de datos
# Esta DATABASE_URL se construye a partir de las variables anteriores y es para referencia.
# La aplicación puede construirla internamente o leerla directamente si está configurada.
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Configuración del Servidor de la Aplicación
# Define dónde se ejecutará el servidor FastAPI
SERVER_HOST=localhost               # Host donde se ejecutará el servidor (usa '0.0.0.0' para Docker)
SERVER_PORT=8000                    # Puerto en el que escuchará el servidor

# Configuración de pgAdmin (Panel de administración para PostgreSQL)
# Valores por defecto si no se especifican en el entorno
PGADMIN_EMAIL=admin@example.com     # Correo para iniciar sesión en pgAdmin
PGADMIN_PASSWORD=admin              # Contraseña para iniciar sesión en pgAdmin
PGADMIN_PORT=5050                   # Puerto donde se ejecutará pgAdmin

# Configuración de OpenAI
OPENAI_API_KEY=your_openai_api_key        # Clave de API de OpenAI para generación de informes

# Configuración de SMTP (correo saliente)
SMTP_HOST=smtp.example.com                # Host SMTP para envío de correos
SMTP_PORT=465                             # Puerto SMTP (465 para SSL, 587 para TLS)
SMTP_USER=your_email@example.com          # Usuario/correo para autenticación SMTP
SMTP_PASS=your_smtp_password              # Contraseña o clave de aplicación SMTP

# API token for report generation tool (required for generate_report)
REPORT_API_TOKEN=changeme-token-dev
